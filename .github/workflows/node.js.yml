name: CI-CD Pipeline

on:
  push:
    branches: [ main,f1,f2 ]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build_version: ${{ steps.set_output.outputs.version }}
    steps:
    - uses: actions/checkout@v3

    # Set up Node.js
    - name: Use Node.js
      uses: actions/setup-node@v3

    # Install dependencies
    - run: npm install

    # Run tests
    - name: Run Tests
      run: npm test

    # Build the application (if applicable)
    - run: npm run build

    # Set an output parameter (example: create a version string)
    - name: Set build version output
      id: set_output
      run: echo "::set-output name=version::$(date +'%Y%m%d%H%M%S')"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    # Mock deployment step using the output from the build job
    - name: Deploy
      run: |
        echo "Deploying version ${{ needs.build.outputs.build_version }}"
        # Add your deployment steps here
