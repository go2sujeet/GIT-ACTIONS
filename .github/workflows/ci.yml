name: The New CI

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
        
      - name: Custom Action
        uses: "./.github/actions/cutom-action"